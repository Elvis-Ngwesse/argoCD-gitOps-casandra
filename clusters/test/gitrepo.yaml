apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: argo-git
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/Elvis-Ngwesse/argoCD-mongodb
  ref:
    branch: main
  timeout: 60s
